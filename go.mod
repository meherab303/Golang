module package_scope

go 1.25.5


// eitake Golang base hishebe dhore.eitake bse dhorei shob path search kore.

// directional structure:
//package_scope(root or module) --->packag_scope(folder)-->er .go file(scope.go) er theke packag_scope(package name) ber kore--->ei packge  er under e function gula access kore.